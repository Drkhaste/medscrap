# راهنمای اجرای ربات مدوفست در اندروید (Termux)

این راهنما به شما کمک می‌کند تا ربات اسکرپر را به صورت کامل روی گوشی اندروید خود نصب و اجرا کنید.

## مرحله ۱: نصب ترموکس (Termux)
بهترین نسخه ترموکس را از **F-Droid** دانلود کنید (نسخه گوگل پلی قدیمی است و کار نمی‌کند).

## مرحله ۲: آماده‌سازی اولیه
برنامه ترموکس را باز کرده و دستورات زیر را به ترتیب وارد کنید:

```bash
# بروزرسانی مخازن
pkg update && pkg upgrade

# نصب ابزارهای مورد نیاز
pkg install proot-distro
```

## مرحله ۳: نصب اوبونتو (برای پایداری بیشتر)
از آنجا که Playwright روی خود ترموکس مستقیم به سختی نصب می‌شود، از یک محیط لینوکس داخلی استفاده می‌کنیم:

```bash
# نصب اوبونتو
proot-distro install ubuntu

# ورود به محیط اوبونتو
proot-distro login ubuntu
```

## مرحله ۴: نصب پایتون و نیازمندی‌ها (داخل اوبونتو)
حالا که وارد محیط اوبونتو شدید (خط فرمان تغییر می‌کند)، این دستورات را بزنید:

```bash
# بروزرسانی اوبونتو
apt update && apt upgrade

# نصب پایتون و ابزارهای لازم
apt install python3 python3-pip git wget

# نصب کتابخانه‌های پایتون
pip3 install playwright aiohttp pandas openpyxl

# نصب مرورگر و پیش‌نیازهای آن
playwright install chromium
playwright install-deps
```

## مرحله ۵: کپی کردن فایل‌های ربات
شما باید فایل‌های `scraper.py` و `termux_main.py` را به داخل گوشی و این محیط منتقل کنید.
ساده‌ترین راه این است که یک پوشه بسازید و فایل‌ها را ایجاد کنید یا از گیت استفاده کنید.

## مرحله ۶: دسترسی به حافظه گوشی (برای ذخیره عکس‌ها در گالری)
در ترموکس (خارج از اوبونتو) ابتدا این دستور را بزنید و اجازه دسترسی بدهید:
```bash
termux-setup-storage
```

## مرحله ۷: اجرای ربات
برای اجرای ربات، در محیط اوبونتو دستور زیر را وارد کنید:

```bash
python3 termux_main.py --url "آدرس_صفحه_سوالات"
```

### نکات مهم:
- **لاگین:** در اولین اجرا، ربات از شما شماره موبایل می‌خواهد. آن را وارد کنید (مثلا 09123456789). سپس کد ۴ رقمی که پیامک می‌شود را وارد کنید.
- **ذخیره نشست:** بعد از یکبار لاگین، فایلی به نام `session.json` ساخته می‌شود. در دفعات بعدی دیگر نیازی به وارد کردن کد نیست.
- **محل ذخیره تصاویر:** تصاویر به صورت خودکار در پوشه `Download/medofast_robot` در حافظه اصلی گوشی شما ذخیره می‌شوند تا در گالری قابل مشاهده باشند.
- **توقف ربات:** برای متوقف کردن ربات از کلید ترکیبی `Ctrl + C` استفاده کنید.

### دستورات تکمیلی:
اگر می‌خواهید بدون انتظار برای پاسخنامه (فقط سوالات و گزینه‌ها) سریع‌تر پیش بروید:
```bash
python3 termux_main.py --url "URL" --skip-exp
```
